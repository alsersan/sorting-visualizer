import React from "react";

const styles = {
  mainContainer: {
    height: "700px",
    width: "1200px",

    backgroundColor: "#ccc",
  },
};

const BarChart = () => {
  return <div style={styles.mainContainer} />;
};

export default BarChart;
